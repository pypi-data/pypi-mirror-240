[project]
name = "kraken_client"
version = "0.1.0"
description = ""
authors = [
    { name = "Fisher60", email = "kyler@abandontech.cloud" },
]
dependencies = [
    "httpx>=0.25.1",
]
requires-python = ">=3.11"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.dev-dependencies]
test = [
    "pytest>=7.4.3",
    "setuptools>=68.2.2",
    "anyio>=4.0.0",
    "pytest-cov>=4.1.0",
    "flake8>=6.1.0",
]

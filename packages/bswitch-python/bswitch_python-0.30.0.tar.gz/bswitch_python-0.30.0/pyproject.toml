[tool.poetry]
name = "bswitch_control"
version = "0.1.0"
description = ""
authors = ["Bar Perach <barp12@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
maturin = ">=1.0,<2.0"

[tool.maturin]
manifest-path = "python/Cargo.toml"

[build-system]
requires = ["poetry-core>=1.0.0", "maturin>=1.0,<2.0", "patchelf"]
build-backend = "maturin"

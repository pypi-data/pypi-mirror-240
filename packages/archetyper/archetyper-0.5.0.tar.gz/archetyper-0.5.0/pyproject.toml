[tool.poetry]
name = "archetyper"
version = "0.5.0"
description = ""
authors = ["Ryan Boyd <ryan@ryanboyd.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[tool.poetry.group.dev.dependencies]
scriv = {extras = ["toml"], version = "^1.5.0"}
tqdm = {extras = ["toml"], version = "^4.66.1"}
nltk = {extras = ["toml"], version = "^3.8.1"}
sentence-transformers = {extras = ["toml"], version = "^2.2.2"}

[tool.poetry.scripts]
my-script = "archetyper.install_punkt:install_punkt"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "interlo"
version = "1.1.2"
classifiers = ["Programming Language :: Python"]
dependencies = ["matplotlib","numpy","galpy","astropy"]
requires-python = ">=3"